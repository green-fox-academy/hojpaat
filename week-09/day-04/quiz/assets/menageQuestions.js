'use strict';

const myRequest = new XMLHttpRequest();

const submitBtn = documen.queryselector('#submitBtn');

submitBtn.onclick = (e) => {
  e.stopPropagation();
  e.preventDefault();

}

function fromData(){
  
}